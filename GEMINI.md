# 개인정보 보호 및 보안 검토 규칙 (Privacy & Security Guidelines)

항상 모든 작업(코드 작성, 파일 생성, git 커밋 및 푸시 등) 시 사용자의 개인정보 및 민감 정보가 외부에 유출되거나 원격 저장소에 올라가지 않도록 다음 원칙을 매우 엄격하게 준수합니다:

1. **개인 식별 정보(PII) 철저한 검토 및 제거**:
   - 실명, 개인 이메일 주소, 전화번호, 상세 주소, 개인 로컬 시스템 경로(`C:\Users\<username>` 등)가 코드, 문서, 발표자료, 커밋 메시지 등에 직접 노출되지 않도록 사전에 반드시 검토합니다.
   - 예시가 필요한 경우 가상의 더미 데이터(`user@example.com`, `https://example.com`)를 사용합니다.

2. **비밀 키 및 인증 정보 보호**:
   - API Key, 토큰(Token), 비밀번호(Password), 인증서 등 보안 자격 증명은 절대 코드에 하드코딩하거나 커밋하지 않습니다.
   - 필요 시 `.env` 또는 환경 변수로 분리하고 `.gitignore`에 등록합니다.

3. **커밋 및 푸시 전 엄격한 사전 검증**:
   - `git add` 및 `git commit` 전 반드시 `git status`와 `git diff`를 검토하여 의도하지 않은 파일이나 임시 파일(OS/오피스 임시 파일, 로그 등)이 포함되지 않았는지 확인합니다.
   - 불필요한 파일이 원격 저장소에 푸시되지 않도록 철저히 차단합니다.

4. **외부 전송 및 공개 전 확인**:
   - 외부 API 호출, 웹 검색, 원격 저장소 push 작업 시 전송되는 데이터에 사용자의 개인정보가 포함되어 있지 않은지 최종 점검합니다.
